module.exports = {
    "extends": "airbnb-base",
    "rules": {
      /* Para evitar subir console.log para o repositorio, use esta regra
      "no-console": ["error", { allow: ["warn", "error"] }]
      */
    }
};
